import { useState } from 'react';
import type { ContextSnapshot } from '../../lib/context/types'; import type { LoadedReadOnlyAgentSnapshot } from '../../lib/readOnlySnapshotLoaderReadiness'; import { runSalesAgentInteraction, type SalesAgentInteractionResult } from '../../lib/salesAgentTools/operatingLayer';
export function SalesAgentInteractionWorkspace({ customerId, snapshot, context, profileId }: { customerId: string; snapshot: LoadedReadOnlyAgentSnapshot; context: ContextSnapshot; profileId: string }) {
  const [message, setMessage] = useState(''); const [result, setResult] = useState<SalesAgentInteractionResult | null>(null); const [state, setState] = useState<'idle' | 'loading' | 'blocked' | 'error'>('idle'); const [error, setError] = useState('');
  const submit = async () => { setState('loading'); setError(''); try { setResult(await runSalesAgentInteraction(message, { customer_id: customerId, snapshot, context, profile_id: profileId })); setState('idle'); } catch (cause) { setState(message.trim() ? 'error' : 'blocked'); setError(cause instanceof Error ? cause.message : String(cause)); } };
  return <section className="card" aria-label="Sales Agent interaction workspace"><h3 className="section-title">Sales Agent</h3><p>Current customer is fixed from this workspace. Tools are read-only; drafts require review; no CRM write or automatic execution.</p><textarea aria-label="Sales Agent message" value={message} onChange={event => setMessage(event.target.value)} placeholder="Ask for a customer assessment or preparation draft." rows={3} style={{ width: '100%' }} /><button className="btn btn-primary" onClick={() => void submit()} disabled={state === 'loading'}>{state === 'loading' ? 'Reasoning…' : 'Ask Sales Agent'}</button>{(state === 'blocked' || state === 'error') && <p role="alert">{error}</p>}{result && <div><p><strong>Human review required.</strong> Drafts are not sent or written.</p><p>Tool trace: {result.trace.map(item => item.tool_id).join(' → ')}</p><p>Evidence: {result.evidence_refs.join(', ') || 'No evidence returned'}</p><p>{result.runtime.result.customer_summary.value}</p></div>}</section>;
}
